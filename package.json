{
  "name": "friendly-stego",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first client-server app to hide little notes inside photos.",
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "install:all": "npm --prefix server install && npm --prefix client install && npm install",
    "test:server": "npm --prefix server test",
    "test:e2e": "playwright test",
    "test": "npm run test:server && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "concurrently": "^9.1.0"
  }
}
